% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_size.R
\name{calculate_sample_size}
\alias{calculate_sample_size}
\title{Calculate Required Sample Size for Target Power}
\usage{
calculate_sample_size(
  target_power,
  effect_name,
  n_obs_per_subj,
  fixed_effects,
  random_effects,
  residual_sd = 1,
  n_subj_range = c(10, 100),
  n_sim = 1000,
  alpha = 0.05,
  seed = NULL,
  verbose = TRUE
)
}
\description{
This function helps determine the number of subjects needed to achieve
a target power level for a specific effect in a mixed effects model.
}
\arguments{
\item{target_power}{Numeric.  Desired power level (e.g., 0.80 for 80\% power).}

\item{effect_name}{Character. Name of the effect to power for. }

\item{n_obs_per_subj}{Integer. Number of observations per subject.}

\item{fixed_effects}{Named numeric vector. Fixed effect coefficients.}

\item{random_effects}{Named numeric vector. Random effect standard deviations.}

\item{residual_sd}{Numeric. Standard deviation of residual error.  Default is 1.}

\item{n_subj_range}{Integer vector of length 2. Range of sample sizes to test. 
Default is c(10, 100).}

\item{n_sim}{Integer. Number of simulations per sample size. Default is 1000.}

\item{alpha}{Numeric. Significance level. Default is 0.05.}

\item{seed}{Integer. Random seed for reproducibility. Default is NULL. }

\item{verbose}{Logical. Whether to print progress.  Default is TRUE.}
}
\value{
A list with class "pwrSim_sample_size" containing:
\describe{
  \item{target_power}{Target power level}
  \item{recommended_n}{Recommended sample size}
  \item{results}{Data frame with power estimates for each sample size}
  \item{effect_name}{Name of the effect that was powered}
}
}
\examples{
\dontrun{
fixed_fx <- c(intercept = 5, effect1 = 0.5, effect2 = 0.3, interaction = 0.4)
random_fx <- c(subj_intercept = 1.0, subj_slope = 0.5)

ss_result <- calculate_sample_size(
  target_power = 0.80,
  effect_name = "interaction",
  n_obs_per_subj = 20,
  fixed_effects = fixed_fx,
  random_effects = random_fx,
  n_subj_range = c(20, 50),
  n_sim = 100,
  seed = 123
)

print(ss_result)
plot(ss_result)
}
}